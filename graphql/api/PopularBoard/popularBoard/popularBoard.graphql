type Query {
  getPopularBoard(
    searchValue: String!
    limit: Int!
    currentPage: Int!
  ): [PopularBoard!]!

  getPopularDetail(id: String!): PopularBoard!
  getPopularTotalPage(searchValue: String!, limit: Int!): Int!
  getPopularTotalPageOnlyCnt(searchValue: String!, limit: Int!): Int!
}
